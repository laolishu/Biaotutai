## 1. Shape 识别与标准化

- [x] 1.1 在 `shape` 解析流程中新增结构化判定（优先识别 `rect`/`card`）
- [x] 1.2 为结构化 shape 输出 `props.shapeType`、`props.fill`、`props.cornerRadius`、`props.stroke`
- [x] 1.3 为无法结构化的复杂路径增加降级分支与降级标记字段（如 `degraded`、`degradeReason`）

## 2. 资源降级与导出

- [x] 2.1 为复杂路径降级图层生成图片资源并写入 `assets` 元数据对象
- [x] 2.2 确保降级图层通过 `assetId` 正确关联 `assets` 键并通过一致性校验
- [x] 2.3 更新导出链路，保证 `assets` 始终输出元数据对象结构

## 3. UI 与文档对齐

- [x] 3.1 更新图层面板展示，支持显示 `shapeType` 与降级状态信息
- [x] 3.2 更新属性面板字段映射，展示 shape 的颜色、圆角、边框信息
- [x] 3.3 补充对接说明中的降级策略与兼容规则（解析器处理建议）

## 4. 验证与回归

- [ ] 4.1 增加/更新样例数据覆盖：规则矩形、卡片样式、复杂路径三类输入
- [ ] 4.2 执行导出与导入联调，确认结构化与降级路径均可被正确解析
- [ ] 4.3 记录风险与回滚策略验证结果（必要时切回全量降级路径）