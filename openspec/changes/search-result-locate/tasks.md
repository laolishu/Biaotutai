## 1. 双击定位交互

- [x] 1.1 在 `src/components/LayerSearchResults.vue` 的搜索结果列表项上添加 `@dblclick` 事件处理函数 `locateLayer(layer)`：调用 `store.selectLayer(layer.id)` 后再调用 `store.setSearchQuery('')`

## 2. 验证

- [x] 2.1 运行 `npm run build` 确认无构建错误
