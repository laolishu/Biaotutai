## 1. 数据与标准化准备

- [x] 1.1 盘点 image 图层当前可用字段，确定缩略图来源优先级（图层预览 URL / 解析 canvas / 占位）
- [x] 1.2 在 src/utils/normalizeLayer.js 中补充 image 预览字段标准化（可选字段，缺失不报错）
- [x] 1.3 在 src/store/index.js 保证 selectedLayer 可返回 image 预览相关字段给右侧属性栏

## 2. 右侧属性栏 UI 实现

- [x] 2.1 在 src/views/Workbench.vue 的属性分页中新增 image 类型专属缩略图属性区块
- [x] 2.2 缩略图区块展示缩略图、尺寸信息与加载失败占位提示
- [x] 2.3 缩略图支持点击放大预览（轻量弹层/浮层），支持关闭返回

## 3. 交互与样式完善

- [x] 3.1 为 image 缩略图区块补充样式，保证与现有属性面板视觉一致
- [x] 3.2 处理非 image 类型分支，确保不展示缩略图区块
- [x] 3.3 验证资源缺失、加载失败、切换图层等边界场景不影响属性面板稳定性

## 4. 验证

- [x] 4.1 选中 image 图层时显示缩略图属性并可点击放大预览
- [x] 4.2 选中非 image 图层时不显示缩略图区块
- [x] 4.3 预览资源不可用时显示占位与提示，页面不报错
